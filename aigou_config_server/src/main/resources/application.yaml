server:
  port: 1299
eureka:
  client:
    service-url:
      defaultZone: http://localhost:7001/eureka
  instance:
    prefer-ip-address: true
spring:
  application:
    name: provider-config
  cloud:
    config:
      server:
        git:
          uri: https://github.com/dustfallp/java_config.git  #git文件地址
          username: dustfallp
          password: pj19971005